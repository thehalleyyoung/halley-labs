{
  "function": "abs",
  "tier": 1,
  "preconditions": [
    { "formula": "true", "provenance": "absolute value is total; no precondition needed" }
  ],
  "postconditions": [
    { "formula": "ret >= 0",              "provenance": "non-negativity; mutant M4 (UOI: -x → x) killed" },
    { "formula": "(x >= 0) -> ret == x",  "provenance": "positive identity; mutant M5 (UOI: x → -x) killed" },
    { "formula": "(x < 0) -> ret == -x",  "provenance": "negation of negative input; mutants M2, M3 (ROR) killed" }
  ],
  "mutation_operators": ["ROR", "UOI"],
  "mutants_killed": 5,
  "verified": true
}

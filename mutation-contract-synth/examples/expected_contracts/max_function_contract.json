{
  "function": "max",
  "tier": 1,
  "preconditions": [
    { "formula": "true", "provenance": "max is total over all integer pairs" }
  ],
  "postconditions": [
    { "formula": "ret >= a",              "provenance": "result dominates first argument; mutants M2, M5 (ROR) killed" },
    { "formula": "ret >= b",              "provenance": "result dominates second argument; mutants M3, M4 (ROR) killed" },
    { "formula": "ret == a || ret == b",  "provenance": "result is one of the inputs; mutant M1 (a >= b → a > b) killed" }
  ],
  "mutation_operators": ["ROR"],
  "mutants_killed": 5,
  "verified": true
}

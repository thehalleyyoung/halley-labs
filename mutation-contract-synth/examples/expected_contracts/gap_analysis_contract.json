{
  "function": "safe_divide",
  "tier": 1,
  "preconditions": [
    { "formula": "divisor != 0", "provenance": "gap_analysis: mutants M1-M3 on guard `divisor == 0` require this precondition to be killed" }
  ],
  "postconditions": [
    { "formula": "(divisor > 0 && dividend >= 0) -> ret >= 0", "provenance": "sign agreement: positive quotient for same-sign operands" },
    { "formula": "(divisor > 0 && dividend < 0) -> ret <= 0",  "provenance": "sign agreement: negative quotient for opposite-sign operands" },
    { "formula": "ret >= -3",                                   "provenance": "bounded quotient lower bound (unrolled to 3)" },
    { "formula": "ret <= 3",                                    "provenance": "bounded quotient upper bound (unrolled to 3)" }
  ],
  "mutation_operators": ["ROR", "AOR", "UOI"],
  "mutants_killed": 8,
  "verified": true
}

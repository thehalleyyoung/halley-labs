{
  "function": "max",
  "tier": 1,
  "preconditions": [
    {
      "clause": "true",
      "strength": "trivial",
      "note": "Max is total over all integer pairs."
    }
  ],
  "postconditions": [
    {
      "clause": "ret >= a",
      "strength": "tight",
      "note": "Result is at least as large as the first argument."
    },
    {
      "clause": "ret >= b",
      "strength": "tight",
      "note": "Result is at least as large as the second argument."
    },
    {
      "clause": "ret == a || ret == b",
      "strength": "tight",
      "note": "Result is one of the two inputs."
    }
  ],
  "provenance": {
    "mutants_killed": [
      { "id": "M1", "operator": "ROR", "original": "a >= b", "mutant": "a > b"  },
      { "id": "M2", "operator": "ROR", "original": "a >= b", "mutant": "a <= b" },
      { "id": "M3", "operator": "ROR", "original": "a >= b", "mutant": "a == b" },
      { "id": "M5", "operator": "ROR", "original": "a >= b", "mutant": "a < b"  }
    ],
    "synthesis_method": "lattice_walk",
    "lattice_tiers_explored": 1,
    "z3_queries": 8,
    "wall_time_ms": 19
  }
}

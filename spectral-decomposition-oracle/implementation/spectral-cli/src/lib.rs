//! spectral-cli library: shared types and utilities for the CLI.
//!
//! The CLI binary (`spectral-oracle`) uses this library for command
//! implementations, shared configuration, and output formatting.

pub mod commands;
